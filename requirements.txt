# Core
requests>=2.31.0
python-dotenv>=1.0.0

# Premium model SDKs (install only what you use)
anthropic>=0.24.0          # Claude - pip install anthropic
google-generativeai>=0.3.0 # Gemini - pip install google-generativeai

# Local models work out of the box via HTTP (no extra packages needed)
# Ollama:   https://ollama.ai (free, run locally)
# LM Studio: https://lmstudio.ai (free, GUI for local models)
# vLLM:     https://github.com/vllm-project/vllm (free, high performance)
